import AdminLayout from '@/components/AdminLayout';
import React from 'react';

type Props = {};

const EditStock = (props: Props) => {
  return <AdminLayout title='Редактирование предмета'></AdminLayout>;
};

export default EditStock;
